<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:extras="clr-namespace:Extras;assembly=Extras">

    <!-- Extras Theme Styles -->
    
    <!-- Banner Grid Style -->
    <Style x:Key="BannerGridStyle" TargetType="Grid">
        <Setter Property="Height" Value="100"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
    </Style>
    
    <!-- Metadata Display Style -->
    <Style x:Key="MetadataTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{DynamicResource FontSize}"/>
        <Setter Property="Foreground" Value="{DynamicResource ExtrasHighlightBrush}"/>
        <Setter Property="Margin" Value="0,5,0,0"/>
    </Style>

</ResourceDictionary>
